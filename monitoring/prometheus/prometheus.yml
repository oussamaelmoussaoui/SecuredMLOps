global:
  scrape_interval:     30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: "evidently_drift"
    static_configs:
      - targets: ["evidently_exporter:8001"]
    metrics_path: "/metrics"
    scrape_interval: 30s
